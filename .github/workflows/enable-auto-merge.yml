name: Disabled: enable-auto-merge (deprecated)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated; auto-merge is handled by .github/workflows/auto-pr-and-automerge.yml"
